# AutoLock 설치 가이드 (동료용)

이 앱은 Apple 공증(notarization) 없이 배포되었습니다(self-signed 서명). 그래서
처음 실행할 때 macOS Gatekeeper가 한 번 막습니다. 아래 절차로 한 번만 허용하면
이후로는 정상 동작합니다. (v0.5.0부터 고정 self-signed 인증서로 서명해, 자동
업데이트로 앱이 교체돼도 권한이 유지됩니다 — 자세한 내용은 README의 "업데이트 확인".)

## 요구사항

- **macOS 13 (Ventura) 이상**
- **Apple Silicon (M1 이상)** — Intel Mac 미지원

## 1. 설치

### DMG로 받았다면

1. `AutoLock-x.y.z-arm64.dmg` 더블클릭
2. 마운트된 창에서 **AutoLock.app**을 **Applications** 폴더로 드래그
3. DMG는 마운트 해제(이젝트)

### ZIP으로 받았다면

1. `AutoLock-x.y.z-arm64.zip` 더블클릭으로 압축 해제
2. 나온 **AutoLock.app**을 **Applications** 폴더로 이동

## 2. 첫 실행 (중요)

그냥 더블클릭하면 다음 경고가 뜹니다:

> "AutoLock.app"은(는) Apple에서 확인되지 않은 개발자가 배포한 앱이므로 열 수 없습니다.

이건 정상입니다. 두 가지 방법 중 하나를 쓰세요.

### 방법 A: Finder에서 우클릭 → 열기 (권장)

1. Applications 폴더에서 **AutoLock.app**을 **우클릭**(또는 Control+클릭)
2. 메뉴에서 **열기** 선택
3. 동일한 경고가 다시 뜨지만, 이번엔 **열기** 버튼이 있음 → 클릭
4. 한 번만 하면 다음부턴 더블클릭으로 그냥 열림

### 방법 B: 시스템 설정에서 허용

1. 더블클릭해서 차단 경고를 보기
2. **시스템 설정 → 개인정보 보호 및 보안** 열기
3. 하단에 "AutoLock.app이 차단되었습니다"가 보이면 옆의 **"확인 없이 열기"** 클릭

### 방법 C (응급): 격리 속성 제거

위 두 방법이 안 먹히면 터미널에서:

```bash
xattr -d com.apple.quarantine /Applications/AutoLock.app
```

## 3. 권한 허용

처음 활성화할 때 macOS가 묻습니다:

- **Bluetooth 사용 권한** → **확인** 또는 **허용**
  (시스템 설정 → 개인정보 보호 및 보안 → Bluetooth에서도 확인 가능)

거부하면 BLE 스캔이 안 되어 앱이 동작하지 않습니다.

## 4. 사용

1. 메뉴바 우측 상단의 자물쇠 아이콘 클릭
2. 우상단 토글로 활성화
3. **로그인 시 자동 시작** 토글 ON (선택)
4. 폰을 가까이 두기. 별도 앱 없이 시스템 BLE 신호로도 동작은 하지만,
   Apple 기기는 식별자가 주기적으로 회전하므로 상시 사용에는 신호 송출 앱 권장:
   - iPhone / Android: **nRF Connect for Mobile** → Advertiser 탭
   - 가장 안정적인 대안은 **Apple Watch Auto Unlock**
5. **+ ADD** 버튼 → 라이브 스캔 목록에서 본인 디바이스 선택
6. 거리 임계값 슬라이더 조정
   - 거리 임계값: −40 ~ −100 dBm (10 단위, 기본 **−80 dBm**)
   - 즉시잠금 임계값은 `거리 임계값 − 10 dBm`로 자동 산정
   - **신호 끊김 허용은 10초로 고정**되어 있습니다(v0.4.0부터 슬라이더 제거)

## 5. 업데이트

앱을 켜면 GitHub에서 새 버전을 자동 확인합니다(메뉴 하단 버전 클릭으로 수동 확인도 가능).

- 새 버전이 있으면 메뉴에 **"새 버전 vX.Y.Z — 업데이트"** 버튼이 뜹니다.
- 누르면 ZIP 다운로드 → SHA256 검증 → **앱 자동 교체 → 재시작**까지 한 번에 끝납니다.
  더 이상 수동 드래그가 필요 없습니다.
- v0.5.0부터 고정 서명이라 **업데이트 후에도 Bluetooth·접근성 권한이 유지**됩니다.

> ⚠️ **v0.4.0 이하 → v0.5.0** 이번 한 번만: v0.4.0 업데이터는 DMG를 열어주므로
> 위 "1. 설치"대로 수동 설치하고, 서명 방식 변경으로 **Bluetooth·접근성 권한을 1회
> 다시 허용**해야 합니다. 이후 v0.5.0 → v0.5.1부터는 권한 유지된 채 자동 교체됩니다.

## 6. 자동 해제 (Apple Watch)

해제 방식은 두 가지가 있습니다.

### 방식 A: Apple Watch Auto Unlock (권장 — macOS 기본 기능)

이건 Apple이 제공하는 시스템 기능입니다 (AutoLock과 무관). AutoLock이 잠그고,
macOS가 워치로 풀어주는 분담:

1. 시스템 설정 → 잠금 화면
2. **"Apple Watch로 Mac 잠금 해제 가능"** 직접 활성화

가장 안정적이고 보안적으로 깔끔합니다.

### 방식 B: 자동 잠금 해제 (실험적, Apple Watch 없이)

저장된 로그인 암호를 잠금 화면에 자동 입력하는 방식. macOS 보안 정책상 환경에
따라 동작하지 않을 수 있어요.

1. 메뉴 → **근접 시 자동 잠금 해제** 토글 ON
2. 펼쳐지는 박스에서:
   - **로그인 암호** → "설정" 클릭 → 별도 창에서 암호 입력 → 저장
     (Keychain에 로컬 저장, 외부 전송 없음)
   - **접근성 권한** → "허용" 클릭 → 시스템 설정 → 손쉬운 사용에서 AutoLock 활성화
3. 두 항목이 모두 초록 체크가 되면 준비 완료
4. 잠금 후 충분히 가까워지면 자동으로 풀립니다 (잠금 임계값 + 10 dBm 이내)

**주의**
- 잠금 화면 보안 정책상 일부 macOS 환경에선 화면 깨우기까지만 됩니다.
- 암호의 무인 읽기는 현재 AutoLock 서명으로 제한됩니다. 이전 버전에서 저장한
  암호는 보안 마이그레이션 과정에서 삭제될 수 있으며, 이 경우 한 번 다시 입력하세요.
- 토글을 끄거나 "저장된 암호 삭제"를 누르면 Keychain에서 즉시 제거됩니다.

## 7. 제거

1. AutoLock 메뉴 → "로그인 시 자동 시작" OFF
2. AutoLock 메뉴 → 종료
3. **Applications/AutoLock.app**을 휴지통으로 이동
4. (선택) 설정/권한 정리:
   ```bash
   defaults delete com.local.autolock
   tccutil reset Bluetooth com.local.autolock
   tccutil reset Accessibility com.local.autolock
   security delete-generic-password -s com.local.autolock.unlock 2>/dev/null
   ```

## 문제 해결

| 증상                          | 해결책                                                          |
| ----------------------------- | ---------------------------------------------------------------- |
| 메뉴바 아이콘이 안 보임       | Activity Monitor에서 AutoLock 프로세스 확인. 죽었으면 재실행     |
| "Bluetooth 꺼짐" 표시         | Bluetooth ON                                                     |
| "권한 없음" 표시              | 시스템 설정 → 개인정보 → Bluetooth → AutoLock 활성화             |
| 디바이스가 스캔에 안 잡힘     | 폰의 신호 송출 앱이 켜져 있는지 확인. iOS는 백그라운드 제약 있음   |
| 너무 자주 잠김                | 거리 임계값을 더 먼 값으로 (예 −90)                              |
| 잠겨야 하는데 안 잠김         | 거리 임계값 더 높은 값으로 (예 -70)                              |

## 보안 메모

- 이 앱은 Apple에 notarize되지 않았습니다. 신뢰할 수 있는 출처(이 저장소/제작자)에서만 받으세요.
- Bluetooth 권한 외에 다른 시스템 권한은 요구하지 않습니다.
- 모든 데이터는 로컬 UserDefaults에만 저장됩니다(외부 전송 없음).
